# 立项卡：RGTC 落地 —— 从"枚举禁止"到"三不变式"（RG-01 ~ RG-05）

> 状态：**RG-01、RG-02 已实施；RG-03 / RG-04 的第一阶段（只测量 / 只分类）已实施**（2026-09-26）；
> 两个第二阶段的判据翻判与 RG-05 **规划中**（各自有前置门：G1 误报数据 / G2 分类清单 / G3 成本）。
> 本卡只定"改什么、怎么验、什么不做"。一个包一个可独立回滚的提交。
> 编号空间：`RG-` 是**新命名空间**，不与 `SEC-` / `Q-` / `R-` / `REL-` / `H-` 共享编号（吸取撞号教训）。
> 来源：`C:\Users\69215\Desktop\RGTC-paper`（`REPORT.md` + `从枚举禁止到结构不变式.md` + 原型
> `rgtc_kernel.js` / `redteam.js`）。原型数字由**本工作区独立重跑**复核：攻击面 8/8、合法面 7/7、
> 证据链 5/5、台账 29 条链完整、影子 8 次 —— 与作者给的 `run-final.txt` 一致。
> 与 BACKLOG 的关系：不回填、不改其状态。本卡是**新一批**。
> **本卡头部不手抄断言总数**：条数以 `python test_all.py` 的输出为准。

---

## 0. 一句话

> 把安全的**对象**从"命令/工具名"换成"世界的三条性质"：**世界可重建（I1）· 意图可归属（I2）·
> 范围可核对（I3）**。三条成立时，agent 做什么都不需要被预先枚举 —— 于是"保留全部能力"与
> "安全"不再互斥，评价指标也从"拦掉多少危险命令"变成两个可测的数：**漏报 / 误报**。

这份立项卡只做**它的落地部分**，并且明确划掉本方案自己承认解决不了的东西（见 §6）。

---

## 1. 目标与验收指标

| 指标 | 现状（ACE v3.42.0） | 本卡完成后的目标 | 怎么量 |
|---|---|---|---|
| 漏报 | 名单＋正则，自认补不全 | 注入类与"改安全网"类用例**全拦** | `SECURITY-AUDIT.md` §七 payload 清单逐条对账 |
| 误报 | O(危险步数) 弹窗 | 编辑类动作 **O(任务数)** | 真实会话回放：弹窗次数/任务 |
| 信任锚位置 | 项目目录内（`.guardian/signing_key`） | 工作区外 + OS 权限 | 路径断言 + 篡改检测 |
| 历史可核验 | append-only，无 MAC | 链式签名，改/删/插可见 | 三条篡改用例 |
| 能力回归证据 | 无 | "原本要问人的动作零弹窗"的可复核清单 | 影子/分类器只读清单（§5） |

**注意**：本卡**不**承诺降低总弹窗数到某个具体值 —— 那要在 RG-04 的测量门之后才有数。

---

## 2. 语言与组件决策（先定这个）

**决定：不引入任何新语言，不引入 C++。**

| 做什么 | 用什么 | 为什么 |
|---|---|---|
| 判定、策略、台账、授权令、来源归属 | **Python（纯 stdlib）** | 与执行层同一个进程、同一套 `test_all` 纪律；HMAC/JSON/路径判断都在 stdlib 里，零新依赖 |
| 纯计算（指纹、哈希、去重、事件索引） | **已有的 Rust `engine/`** | 它今天的边界就是"只算不裁"（`engine/README.md`）；扩大它的角色＝破坏这条边界，所以**只**接纯计算 |
| 真需要**进程边界**时 | **已有的 Go `executor/`** | Job Object / 容器已经在那一侧；不另造一个"内核进程" |
| 影子平台的廉价复制（若 RG-05b 通过成本门） | **Python + `ctypes`** 调 ReFS 块克隆 | 一个 API 调用，不需要新工具链；Rust 侧只做指纹 |

三条**否决**理由，写下来免得以后有人再提：

1. **不新增语言**：ACE 已经有三套（Python / Rust / Go）且各有明确边界；第四套只会带来第四条发布链
   （又一条"本地不可验证"的路，本仓库在这上面已经红过五次）。
2. **不用 C++**：这里没有性能诉求需要它（真需要就把活交给已有的 Rust 引擎），而它会引入构建工具链、
   ABI、跨平台发布三个新变量。
3. **不把"内核"写成又一个 Python 模块**：agent 与执行层**同进程、同用户**，同进程的东西不构成信任边界
   （见 §3）。

---

## 3. 座位（seat）在哪 —— 本卡对原方案最大的一处修改

原报告的核心主张之一是"席位分离"：承载信任的机制必须住在被信任对象够不着的地方。
今天 ACE 的实情（已核实）：

- `core/guardian.py:91`：`self.store = project_root / ".guardian"` —— **在项目目录里**；
  `:101-106`：签名密钥落在 `.guardian/signing_key`。
- `core/sensitive.py:92-98`：`AGENT_STATE_DIRNAMES = {".guardian", ".ace_sessions", ".agent_flywheel",
  ".poc_reports"}`，理由原文就是"agent 改一行 `meta.json` 就能让 `verify_snapshot` 失效"。
- `tools/file_ops.py:102` → `_resolve_target_path` → `sensitive_target`：**读写都过这道判据**
  （`test_all.py:1889/1912` 有断言）。这道守卫是 2026-09-25 的 `b6aeede` 进来的。

**结论**：今天保护信任锚的是一道**路径判据**，而路径判据正是本方案论证"补不全"的那一类东西。
但**修法不是写一个内核模块**：

> 座位 = **工作区之外的、由操作系统权限保护的状态目录**（数据层），加上**已有的进程边界**
> （需要在同一进程里做不了的事时，走 `executor/`）。

同用户、同进程的 agent，靠文件位置**不能**获得绝对边界 —— 这一点必须写在文档里而不是含糊过去。
真正的边界仍是"agent 够不着工作区外"这条**执行层的既有约束** + OS ACL；进程隔离是加强项，不是本卡的前提。

---

## 4. 工作包

每个包：改动点 / 可证伪验收 / 风险 / 回滚 / 规模。**全部按"一个包一个可独立回滚的提交"落地。**

### RG-01 · 信任锚外移（S）—— **已实施**

- **改动**：`core/guardian.py` 的密钥路径解析改为**工作区之外的锚目录**
  （Windows `%LOCALAPPDATA%\ace-agent\state\<sha256(项目绝对路径 normcase)[:16]>\signing_key`；
  POSIX `$XDG_STATE_HOME` 或 `~/.local/state` 同构），根目录可用构造参数 `anchor_dir` 或
  环境变量 `ACE_ANCHOR_DIR` 注入。**快照仍留在 `.guardian/snapshots/`**（那是数据不是锚）。
- **实施记录（含对规划的一处修正）**：
  1. 规划里写的是"旧密钥保留但不再使用" —— **错**，已改为**搬完即删**。只拷不删等于把同一把密钥
     继续留在 agent 可写处，锚白搬（这正是"实测比设计更有话语权"的一例：先写了探针才看出来）。
     删除前先写锚 + 读回校验；删不掉就大声告警（此时边界并未真正建立）。
  2. 锚不可用时 `snapshot()` **直接抛错拒绝**（走执行层既有的"快照不可用 → 503"），
     而不是生成一份未签名快照 —— 未签名快照的摘要可以随内容一起被改，等于把安全网变装饰。
  3. 另补一处 fail-open：快照**带签名**、而当前拿不到密钥时，`verify_snapshot()` 从原来的
     "跳过签名只比摘要"改为**直接拒绝**（原文：拿不到密钥就说拿不到）。
  4. POSIX 下密钥文件 `chmod 0600`（Windows 靠 `%LOCALAPPDATA%` 的 ACL）；设不上只告警不中断。
- **验收（可证伪，全部已落地为 `test_all` 段 `[71]` 的 RG-01a~g）**：
  1. 迁移：`anchor_migrated=True`、锚里是同一把密钥、**项目内那份已不存在**；
  2. 迁移后老快照 `verify_snapshot` 仍为 true；
  3. 锚路径不以项目路径为前缀；
  4. **伪造不成立**：改副本 + 修 `meta.json` 摘要 → 仍判坏快照（项目内已无密钥可重签）；
  5. 攻击者自带一把密钥重签 → 仍不通过（锚里那把才是真的）；
  6. 拿不到密钥时拒绝验证带签名的快照；
  7. 锚不可用 → `snapshot()` 抛 `SnapshotError`（fail-close）。
- **可运营性（2026-09-26 深化）**：锚现在是**硬依赖**（不可写 → 写操作 fail-close），所以
  `python -m cli.ace_doctor` 增加信任锚体检：报锚路径与已有哪些密钥、锚根来自默认还是
  `ACE_ANCHOR_DIR`、**锚是否落在工作区内**（与设计相反时告警）、**项目内是否还残留旧密钥副本**
  （"搬了等于没搬"的那种）、POSIX 下密钥文件权限是否过宽。断言 `RG-01h` 钉住它真的报出来。
- **实测对照（探针 `e2e/rg_probes.py rg01`）**：修复前"改内容+修摘要+用项目内密钥重签"
  → `verify=True`（安全网可伪造）；修复后同一操作 → `verify=False`（签名校验失败）。
- **顺带修正的文档**：`docs/SECURITY-AUDIT.md` 的 SEC-010 记录描述的
  `resolve_signing_key()` / `ACE_SIGNING_KEY` / `~/.ace/snapshot_signing_key` /
  `get_stats()["snapshot_signing"]` **在代码里 0 命中**，而实际把密钥放在了项目内 ——
  该段已加勘误（描述的设计未落地，且与代码事实相反）。
- **风险**：低。**回滚**：`git revert` 该提交（迁移只搬不删旧快照，锚里的密钥是数据的副本）。

### RG-02 · 事件台账链式签名（S）

- **改动**：`cli/ace_sessionlog.py` 每条事件加 `mac = HMAC(anchor, seq ‖ prev_mac ‖ canonical(body))`；
  `/audit` 增加整链校验；`seq` 断裂检测已有（`test_all` E4）。
- **验收**：
  1. 改一行内容 → 链断，且指出是哪条 `seq`；
  2. 删中间一条 → `seq` 断 + 链断；
  3. 尾部追加伪造行 → 无法生成有效 `mac`；
  4. **老日志（无 mac）→ 如实报"不可核验（早于本版）"**，既不判 ok 也不判失败（不静默降级）；
  5. 量一次开销：每事件 HMAC < 50 µs（**量出来的数写进卡里**，不写"应该很快"）。
- **风险**：低。**回滚**：签名开关（默认开）关掉即回到 append-only。

### RG-02 · 事件台账链式签名（S）—— **已实施**

- **改动**：`cli/ace_sessionlog.py` 每条事件带 `mac = HMAC(台账密钥, prev_mac ‖ 该条正文)`；
  正文按"排序键 + 紧凑分隔符"规范化（`_canonical_body`），所以键序变化不影响校验。
  台账密钥从**同一个锚**派生（`<锚>/sessionlog_key`，再 HKDF 式的域分离 `ace-sessionlog-v1`），
  与快照签名密钥**分开**：各用各的，互不牵连。`sessions.md` 与 `ai_code.py` 的
  `/audit stats` 增加整链校验行（三态：ok / broken / unverifiable）。
- **实施记录**：
  1. 校验接口返回**三态**而不是布尔：`"ok"` / `"broken"` / `"unverifiable"`（老日志或拿不到密钥）
     / `"empty"`。把"验过了没问题"与"根本没法验"混在一起，是这类机制最常见的失效方式。
  2. **一处刻意的偏离**：台账密钥不可用时**不 fail-close**，而是照写、打一次告警、
     校验如实报 `unverifiable`。理由：台账是**记录**不是闸门 —— 因为写不了签名就让整轮对话挂掉，
     换不来任何安全收益（攻击者本来就能删掉整份日志）。这与快照（闸门，必须 fail-close）不同，
     所以两条路故意不一样，且都写在代码注释里。
  3. 被既有测试抓到一次回归：`append()` 里**先算 MAC 后验序列化**，把"不可序列化 payload 抛
     `ValueError`"这条既有契约换成了 `TypeError` 漏出。已改为先兑现深冻结契约再算 MAC。
- **验收（`test_all` 段 `[71]` 的 RG-02a~g + H + i）**：
  a 正常日志整链 ok 且每条带 mac；b 改内容 → broken 并指出第几条；c 删中间一条 → broken
  （不只是 seq 缺口）；d 剥掉某条的 mac → broken；e 尾部追加伪造 → broken（**seq 连续也照样抓到**）；
  f 老日志（无 mac）→ `unverifiable`，不冒充 ok 也不冤枉成 broken；g 密钥不可用 → 不炸 + `unverifiable`；
  i/i2 **显示**这一半也验：`/audit stats` 真的打出 ok 行、台账被改后打出 broken 行
  （上一轮 I5/I6 的教训：只测记录不测显示，等于没测用户看到的东西）。
- **实测（探针 `e2e/rg_probes.py rg02`）**：三个真攻击形状——改 `permission/decision`
  （deny→allow）、删中间一条、尾部追加伪造事件——修复前 `seq_contiguous()` 只抓得到第二个，
  日志里**没有任何字段**能说明被动过；修复后三条全部 `verify_chain() = broken` 并指出位置。
- **开销（量出来的，不是"应该很快"）**：单条 MAC 计算 **6.5 µs**；`append` 整条路径 **11.4 ms/条**，
  其中绝大部分是**既有的** `os.fsync`（本次新增的不是它）。
- **风险**：低。**回滚**：`git revert` 该提交；旧日志（无 mac）本就被判 `unverifiable`，不会误伤。

### RG-03 · 来源归属：把注入从"越权"降级为"噪声"（M）—— **第一阶段（只测量）已实施**

**先复现（探针 `e2e/rg_probes.py rg03`）**：同一句 `file_delete(notes.txt)`，只改 `user_input`：

| 场景 | `user_input` | 裁决 | 文件 |
|---|---|---|---|
| A 用户明确要求 | "帮我把 notes.txt 删了" | **SUCCESS** | 被删 |
| B 用户只让看 README（删除指令来自读到的外部内容） | "看看 README 里安装说明写了什么" | **SUCCESS** | 被删 |
| C 用户完全没提过这个文件 | "今天几号" | **SUCCESS** | 被删 |

三条**一模一样**、零弹窗 —— 执行层的输入只有 `(agent_output, user_input)`，工具结果里的外部内容
根本不在它的输入里，所以它回答不了"是谁让做的"。

**第一阶段（本次）只测量，不改裁决**：

- 新增 `core/ace_taint.py`：`TaintLedger` 记"用户说过什么 / 读了哪些外部内容（`EXTERNAL_TOOLS`）/
  哪次写入的目标用户提过"；`assess()` 给出 `user` / `unattributed` / `unknown` 三态，
  其中 `unknown`（写类但说不出目标路径，如 `terminal_exec`）**单独计数、不混进**"会问人"里充数。
- 接线：`_stage_new_task` 记用户轮；`_stage_execute` 记工具结果；`_stage_permission` 在**放行那一刻**
  把评估写进既有的 `permission/decision` 事件（可选字段，不传时 payload 与旧版逐字相同 ——
  刻意避免给其余十来处调用点改事件形状，也避免动 Rust 侧的 kind 名单）。
- **数要看得见**：`/audit stats` 增加一行归属分布（`core/ace_taint.attribution_stats()` 从**日志**里数，
  不读内存账本 —— 后者跨会话/重放读不到），三语同步，并注明"测量中，未参与裁决"。
  没有这一行，"测量"就只是个没人读的字段。
- **纪律**：这一阶段**不改变任何裁决**，`RG-03c` 明确断言"同样的注入形状目前仍然放行" ——
  免得把"测量"混成"以为已经修好了"。

**为什么真判据（第二阶段）要等**：I2 需要**模型侧引用**（工具调用里说明"这条动作由哪条用户
观测单授权"），那是协议改动 —— 解析器 / 三份提示词 / mock 剧本 / e2e 都要跟着动，而误报率只有
真数据能回答。所以先按立项卡 **G1 门**收集这个代理指标：写入的目标路径有没有被用户提过。
本次样本很小（A 记到 `user`、B/C 记到 `unattributed`），**不足以**决定开关，需要真实会话数据。

- **验收（`test_all` 段 `[71]` 的 RG-03a~f）**：a 用户提过 → `user` 且 `would_escalate=False`；
  b 用户没提过 → `unattributed` 且 `would_escalate=True`；c **行为未变**（仍放行）；
  d 外部读取与用户轮次都被记下；e 说不出目标 → `unknown` 且不计入 `would_escalate`；
  f `/audit stats` 真的打出归属测量行（G1 的数看得见）。
- **风险**：低（只读测量）。**回滚**：`git revert` 该提交。

### RG-03b · 判据开关（第二阶段，**待 G1 数据**）

- **改动**：
  - 工具结果打来源标签：`file_read` / `search` / `parse_document` / `kb_*` = **EXTERNAL**；用户输入 = **USER**；
    子代理继承父标签；
  - `execution_layer.py` 的 `_stage_permission` 增加判据：写类动作**没有 USER 侧引用 → 升级**；
    引用只来自 EXTERNAL → **升级**；
  - `run_tool_direct()`（`!cmd` / `/review` 回填那条路）**同一判据** —— 否则又是一个绕过口
    （v3.42.0 刚修过同类"绕过执行层"的缺陷）。
- **验收**：
  1. 只引用外部内容 → escalate（对应原型 ATK-1 的形状）；
  2. 无引用 → escalate（ATK-3 形状）；
  3. 用户明确指示的编辑/清理/删除 → 不被误伤；
  4. **已知逃逸必须测出来并记档**：引用一张真实但**不相关**的用户观测单 → 仍通过。
     这条**写成"通过但记档"**，不许写成"已拦住"（§6）；
  5. 误报率：真实会话日志回放，量"原本 auto 现在 escalate"的比例。
- **风险**：中（误报上升）。**回滚**：开关默认 off，先只统计不改裁决；数据够了再开。

### RG-04 · 可逆性分类器（M 测量版；翻判另开 RG-04b）—— **第一阶段（分类器）已实施**

- **改动**：新增 `core/ace_recovery.py`：`RecoveryClassifier` 按**事实**分类
  `GIT`（在版本控制内）/ `SNAPSHOT`（不在 git 但写前快照能覆盖）/ `REGENERABLE`（白名单里的
  依赖与构建产物）/ `NEVER`（凭据 / agent 自身状态 / 版本库自身 / 设备 / 工作区外）/ `UNKNOWN`（说不清）。
  git 事实一次问清（`ls-files -z` 一次、忽略状态走 `check-ignore`），不按文件起进程。
- **两条口径是踩出来的**（都写进模块 docstring）：
  1. **"被 gitignore" ≠ "可再生"**：私有笔记、`secrets/`、`*.local` 同样被忽略，删了不可重建 →
     忽略状态一律判 **UNKNOWN**，只有**白名单目录名**才算 REGENERABLE；
  2. **`sensitive_target` 与 `is_credential_file` 回答的是两个问题**：前者是"工具能不能碰"
     （项目自己的 `.env` 是正常开发对象，所以放行），后者才是"快照有没有备份它"。
     第一版混用了前者，于是 `.env` 被判成"回滚即删除"（= 可逆）—— 被 `RG-04d` 当场抓住。
- **实测（真实工作区，7851 个文件）**：REGENERABLE 6381（81.3%）· UNKNOWN 647（8.2%）·
  NEVER 479（6.1%）· GIT 343（4.4%）· SNAPSHOT 1（0.0%）→ **可重建 85.7%**。
  这一列就是 G2 门要的数：**若开判据，约 8.2% 的目标上的写会从"直接放行"变成"问人"**
  （样本里主要是被 gitignore 的研究产物目录 `.ace-cc-zh/**`）。换句话说：翻判**不是零成本**，
  而这正是先测量再决定的意义。
- **接线与显示（2026-09-26 深化，推翻了本卡初稿"本次不接线"的决定）**：放行事件额外带上
  `recovery`（各目标等级去重后以 `+` 连接）与 `recovery_release`；`/audit stats` 增加一行
  "已分类 N 次 · 可重建会放行 M 次 · 会拦下 K 次（等级明细）"，三语同步，注明"测量中，未参与裁决"。
  理由与 RG-03 那行一样：**G2 的数只能从真会话里来，而没人看得见的数等于没测**（初稿把接线
  押后是过度谨慎 —— 接线本身不改裁决，风险为零）。
- **同轮修掉一个分类缺口**：`git ls-files` 只列**文件**，所以"删掉整个 `src/`"这类**目录目标**
  原本会落到 SNAPSHOT（判错）；现在"目录下有任一被跟踪文件"即判 GIT（`RG-04h` 钉住）。
- **验收（`test_all` 段 `[71]` 的 RG-04a~j）**：a 版本控制内 → GIT；b 白名单目录 → REGENERABLE；
  c **被 gitignore 但不在白名单 → UNKNOWN**；d 凭据 / agent 状态 / 版本库自身 → NEVER；
  e 工作区外 → NEVER、新建目标 → SNAPSHOT；f **反例**：删一个 gitignore 的不可再生目录 →
  `would_release=False` 并点名；g 全部可重建 → `would_release=True`；h **目录目标** → GIT；
  i 放行事件里带上分类；j `/audit stats` 打出这一行。
- **风险**：低（纯函数 + 只读测量）。**回滚**：`git revert` 该提交。

### RG-04b · 判据翻判（L，**待 G2 数据**）

- **改动**：新增 `core/ace_recovery.py`，分类 `GIT / SNAPSHOT / REGENERABLE / NEVER / UNKNOWN`，
  事实全部来自 ACE 已有能力（git 跟踪状态、`EXCLUDE_DIRS`、能否建快照）。
  **第一版只把分类写进日志与 `/audit`，不改任何裁决。**
- **前置门（先测后改）**：跑真实会话 + 全量测试，产出"当前被 deny/escalate、而分类器会判可逆"的清单，
  **逐条人工看**，产出一张"要退化成 NEVER 判据的黑名单条目"表。
- **验收**：① gitignore 目录**不得**自动判 REGENERABLE（可再生要白名单，不能拿"被忽略"当"可再生"）；
  ② 反例测试：`rm -rf` 一个被 gitignore 但不可再生的目录 → 必须 UNKNOWN → escalate；
  ③ 清单里每一条都有结论（无"待看"）。
- **风险**：低（只读）。**规模**：测量版 M；翻判版（RG-04b）L，且只在清单支持时才开。

### RG-05 · 授权令（M/L）与影子平台（L，先过成本门）

- **RG-05a 授权令**：`mandate = (intents, roots, recoveryFloor, irreversibleQuota, ttl)`，用 RG-01 的锚签名。
  验收直接翻原型 ATK-6/7/8：额度用尽 → 问人；越出 roots → 问人；自签/篡改 → 拒。
  收益是弹窗从 O(步数) 到 O(任务数)；**roots 与额度是承重件**，不是装饰。
- **RG-05b 影子平台**：**先量成本，不达标就不做。**
  - 成本门：在真实工作区量"复制 + 跑 + 前后指纹"，建议阈值 **p50 ≤ 300 ms**（依据：本工作区实测
    347 文件 / 11.7 MB 的全量复制 159 ms、建后校验 1.9 s；ACE 真实规模是 1903 文件 / 34.5 MB）。
  - Windows 上没有 overlayfs/APFS clone，廉价复制需要 **ReFS 块克隆**（`ctypes` 调
    `FSCTL_DUPLICATE_EXTENTS_TO_FILE`）；拿不到就**不做**，或只对"未纳入 VCS 的目标"做小范围影子。
  - **明确禁止**：不依据影子放开任意代码执行。原型的外部落点检测是"对 argv 做正则"
    （`rgtc_kernel.js:705-726`），运行时拼出来的路径（`os.homedir()`、`process.env.TEMP`）看不见 →
    会把"写到了工作区外"判成"可逆且在范围内"。所以 `_FORBIDDEN_RULES` 里 `python`/`npm` 那条
    **保留**，直到有真正的强制层（容器/overlay）。

---

## 5. 量测优先的门（本卡的方法论）

三个门，**过不了就停**，不许"先写了再说"：

| 门 | 在哪 | 判据 | 过不了怎么办 |
|---|---|---|---|
| G1 误报门 | RG-03 开关打开前 | 真实会话回放：新增 escalate 比例 ≤ 阈值（先量、后定） | 只统计不改裁决，回去改判据 |
| G2 分类器门 | RG-04b 翻判前 | "拟放开"清单里**零**不可恢复项 | 保留该条黑名单 |
| G3 成本门 | RG-05b 开工前 | 影子 p50 ≤ 300 ms（真工作区实测） | 不做影子，能力回归只走 RG-04 分类器 |

---

## 6. 明确不做（写下来，免得以后被当成"待优化"）

1. **不解决 I2 的语义归属**：模型可以引用一张真实但**不相关**的用户观测单。这是语义问题，不是密码学问题。
   本卡只能把攻击面从"任意注入"缩到"必须存在一张真实用户观测单"。**这是已知逃逸，不是 TODO。**
2. **不批量删除 `core/ace_execpolicy.py` 的 `_FORBIDDEN_RULES`**：它们的定位本来就是"防线之一，不是边界"。
   退一条要有一条的证据（G2）。
3. **不在同进程里造"内核座位"**：同进程同用户不构成边界（§3）。需要强隔离时走 `executor/`。
4. **不引入第四种语言、不用 C++**（§2）。
5. **不搬快照目录**：动它等于同时动 `/undo`、`/rollback`、`gc` 与一堆既有断言，收益却与 I1 无关。

---

## 7. 风险登记

| 风险 | 触发条件 | 应对 |
|---|---|---|
| 误报上升导致体验倒退 | RG-03 开启后 | G1 门；开关可关；拒因文案给"补救路径"（原型 `_remedy` 的做法） |
| `UNKNOWN` 大量出现（未版本控制的遗留项目） | RG-04 上线后 | 文案给"先建凭据再重试"；并如实承认这会把弹窗推回 O(步数) |
| 锚外移后在 CI/冻结包里的路径差异 | RG-01 落地时 | 状态目录必须是可注入的（测试/冻结版各给一条路径），且**取不到时 fail-close**（不许静默用回旧位置） |
| 影子在 Windows 上不可行 | G3 不过 | 放弃 RG-05b，能力回归只靠 RG-04（本卡已经允许这个结局） |
| 自己评自己（不是第三方红队） | 全程 | 复用 ACE 既有的做法：断言 + 可复现命令；**结论只写"当时实测"** |

---

## 8. 与 ACE 既有纪律的接口（必做项）

- 新增文件登记进 `docs/ARCHITECTURE.md` 权威树（守卫 `[38]`）；文档与代码同批提交。
- 新增断言进 `test_all.py`（建议独立段 `[71]`：`RG-` 组），每条对应一个**先复现过**的失败；
  头部**不手抄总数**（守卫 `[39]`）。
- 用户可见文案三语同步（`locales/{zh,en,ja}.json` 占位符一致）。
- 版本与 `CHANGELOG` 按 `core/version.py` 单源；README 徽章同步（守卫 `[68]`）。
- 每个工作包一个可独立回滚的提交；**不静默降级**：任何"取不到锚/影子不可用"都必须 fail-close
  或明确报出降级，不许默默退回旧行为。

---

## 9. 建议顺序

1. **RG-01 + RG-02**（纯加固、零能力损失，一个提交一个包）——直接关掉"信任锚住在被保护对象地盘里"。
2. **RG-03**（先只统计：跑真实会话回放，出 G1 的数据，再决定开不开）。
3. **RG-04 测量版** → 清单 → 才谈 RG-04b。
4. **RG-05a 授权令**（它不依赖影子）。
5. **RG-05b 影子**：先跑 G3 成本测量。**大概率不做**，那就诚实写"不做，理由是实测成本"。

## 10. 出处

- 方案与原型：`C:\Users\69215\Desktop\RGTC-paper`（`REPORT.md` §0/§5/§7/§8/§9；`run-final.txt` 为 UTF-16LE）。
- 复核记录（本工作区）：原型 8/8 · 7/7 · 5/5 · 台账 29 条（独立重跑 `node redteam.js`）。
- ACE 侧现状：`core/guardian.py:91,101-106`、`core/sensitive.py:92-98`、`tools/file_ops.py:102`、
  `cli/ace_sessionlog.py`、`core/ace_execpolicy.py`、`test_all.py:1889/1912`。
- 原型的三处已核问题（作为输入，不是结论）：`stats.auto` 是死计数器（只初始化不自增）；
  影子外落点靠 argv 正则；`file_read` 已受 `sensitive_target` 约束（原报告 §8 步骤 0 的该半条已过期）。
